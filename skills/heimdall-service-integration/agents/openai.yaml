interface:
  display_name: "Heimdall Service Integration"
  short_description: "Secure Heimdall M2M, directory, revocation, and Webhooks"
  default_prompt: "Use $heimdall-service-integration to implement and verify a tenant-scoped BackendService directory, Client lifecycle, and identity invalidation integration."
