interface:
  display_name: "Heimdall Service Integration"
  short_description: "Secure Heimdall M2M, permissions, and revocation"
  default_prompt: "Use $heimdall-service-integration to integrate a tenant-bound BackendService with Heimdall directory, effective user permissions, and fail-closed identity lifecycle handling."
